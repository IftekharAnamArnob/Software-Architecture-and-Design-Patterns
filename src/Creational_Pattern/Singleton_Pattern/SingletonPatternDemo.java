package Creational_Pattern.Singleton_Pattern;

public class SingletonPatternDemo {

    public static void main(String[] args) {

          Singleton.getSingleInstance().printSingleInstance();
          Singleton.getSingleInstance().printSingleInstance();
          Singleton.getSingleInstance().printSingleInstance();

    }
}
